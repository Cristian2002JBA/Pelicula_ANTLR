premiere {
    subtitle("Tabla de Multiplicar del 5:");
    cast tabla = 5;
    cast i = 1;
    cast resultado = 0;
    
    replay (i = 1 ; i < 11 ; i = i + 1) {
        resultado = tabla * i;
        subtitle(resultado);
    }
}
